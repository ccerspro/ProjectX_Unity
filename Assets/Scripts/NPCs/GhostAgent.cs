using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ZhouSoftware
{
    /// <summary>
    /// GhostAgent: Inherits from SampleAgentScript, can be used for ghost-specific behaviors
    /// </summary>
    public class GhostAgent : SampleAgentScript
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}